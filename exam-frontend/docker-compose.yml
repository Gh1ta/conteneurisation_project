version: "2"
services:
  api:
     build : .
     ports:
      - "3000:3000"
  client:
     build: client.txt
     restart: unless-stopped
     ports:
      - "80:80"
     networks:
      - app-network
networks:
   app-network: